<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="/img/cinema.svg">
  <link rel="stylesheet" href="/output.css">
  <title><%= title %></title>
</head>
<body>
  <%- include("partials/navbar") %>
  <div class="px-8 mt-4 flex flex-col items-center justify-center">
    <div class="w-3xl border-black border-2 rounded-lg flex flex-col items-center">
      <div>
        <h1 class="font-bold text-2xl"><%= title %></h1>
      </div>
      <div class="mt-2">
        <form action="/categories/update/<%= category %>" method="POST">
          <div>
            <label for="category">Category</label>
            <input type="text" name="newCategory" value="<%= category %>" required class="border-2">
          </div>
          <div class="my-2 flex justify-center">
            <button type="submit" class="bg-black border-2 border-black text-white px-2 rounded-lg">Update Name</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>