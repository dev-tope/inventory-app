<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="/img/cinema.svg">
  <link rel="stylesheet" href="/output.css">
  <title>Edit: <%= movie.title %></title>
</head>
<body>
  <%- include("partials/navbar") %>
  <div class="px-8 mt-8 flex items-center justify-center">
    <div class="border-2 border-black rounded-lg w-3xl flex flex-col items-center">
      <h1 class="text-2xl font-bold"><%= movie.title %></h1>
      <form action="/movies/update/<%= movie.movie_id %>" method="POST">
        <div class="mt-2">
          <label for="title">Title</label>
          <input type="text" name="title" id="title" value="<%= movie.title %>" class="border-2 border-black ml-8">
        </div>
        <div class="mt-2">
          <label for="genre">Genre</label>
          <input type="text"name="genre" id="genre" value="<%= movie.genre %>" class="border-2 border-black  ml-6">
        </div>
        <div class="mt-2">
          <label for="year">Director</label>
          <input type="text" name="director" id="director" value="<%= movie.director %>" class="border-2 border-black">
        </div>
        <div class="mt-2">
          <label for="year">Year</label>
          <input type="text" name="year" id="year" value="<%= movie.year %>" class="border-2 border-black  ml-8">
        </div>
    
       <div class="mt-2 mb-2 flex justify-center">
         <button type="submit" class="border-2 border-black bg-black px-2 w-28 h-8 rounded-lg text-white">Update</button>
       </div>
      </form>
    </div>


  </div>
</body>
</html>